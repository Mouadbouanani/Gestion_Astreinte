/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Couleurs OCP officielles
        ocp: {
          primary: '#2bca26ff',    // Bleu OCP principal
          secondary: '#157a1aff',  // Bleu foncé
          accent: '#f59e0b',     // Orange/Jaune OCP
          success: '#4eb910ff',    // Vert
          warning: '#239BA7',    // Orange
          error: '#ef4444',      // Rouge
          gray: {
            50: '#f8fafc',
            100: '#f1f5f9',
            200: '#e2e8f0',
            300: '#cbd5e1',
            400: '#94a3b8',
            500: '#64748b',
            600: '#475569',
            700: '#334155',
            800: '#1e293b',
            900: '#0f172a',
          }
        },
        // Couleurs spécifiques aux rôles
        role: {
          admin: '#dc2626',      // Rouge admin
          chef_secteur: '#f59e0b', // Orange chef secteur
          chef_service: '#3b82f6', // Bleu chef service
          ingenieur: '#10b981',    // Vert ingénieur
          collaborateur: '#6b7280' // Gris collaborateur
        }
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        display: ['Inter', 'system-ui', 'sans-serif'],
      },
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
        '128': '32rem',
      },
      animation: {
        'fade-in': 'fadeIn 0.5s ease-in-out',
        'slide-in': 'slideIn 0.3s ease-out',
        'pulse-slow': 'pulse 3s infinite',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideIn: {
          '0%': { transform: 'translateY(-10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
      },
      boxShadow: {
        'ocp': '0 4px 6px -1px rgba(30, 64, 175, 0.1), 0 2px 4px -1px rgba(30, 64, 175, 0.06)',
        'ocp-lg': '0 10px 15px -3px rgba(30, 64, 175, 0.1), 0 4px 6px -2px rgba(30, 64, 175, 0.05)',
      }
    },
  },
  plugins: [],
}
